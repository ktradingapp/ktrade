# KTrade v10.7 — Changes (on top of v10.6)

Applies the full v10.6 production-readiness review (12 items). Built on YOUR
v10.6 — sector cap and all prior fixes preserved.
Full mapping: docs/REVIEW_RESPONSE_v10.7.md.
Tests: 22 + 9 + 6 + 18 + price-sanity, all pass.

## New modules
- data/schema_validation.py   — OHLCV normalize + drop-unclosed-bar (#5, #8)
- data/scan_schema.py         — scan JSON validation (#6)
- risk/position_fills.py      — partial-fill accounting (#9)
- risk/emergency.py           — persistent kill + cancel/flatten (#2)
- risk/state_store.py         — risk-state persistence (#3)
- backend/manual_order_schema.py — dashboard order validation (#1)
- test_v107_fixes.py          — 18 checks for the above

## Changed
- agent/ktrade_agent_v9.py: rank_universe normalizes frames; RiskEngine partial
  fills + serialize/restore/persist state; activate_kill/_close_all wired to the
  emergency controller; CEO wires emergency + state store, restores on startup,
  daily-reset rollover (#4), and halts while kill is active.
- backend/ktrade_alpaca.py: manual /buy /sell routed through RiskEngine + schema
  (#1); STATE_LOCK + snapshot for /all and refresh (#10); fetch_order/
  cancel_all_orders/close_all_positions (#2,#11); PriceStreamer reconnect loop
  (#12); /all validates scan JSON (#6).
- agent/broker_adapter.py: await_fill polls the specific order id + partial
  fills; cancel_all_orders/close_all_positions for the emergency controller.
- ktrade_vectorbt.py: walk-forward shifts signals one bar (#7 immediate).
- requirements.txt: pydantic>=2.7.0.

## Still open by design
- True train/test walk-forward optimization (#7 deeper rewrite).
- API retry/backoff; real 1-share Alpaca paper round-trip.
- Keep KTRADE_PAPER_ORDER_SUBMISSION=false and LIVE_TRADING=false until verified.
