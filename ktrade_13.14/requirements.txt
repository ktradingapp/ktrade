# KTrade PRO v12.8 — Python Dependencies
# pip install -r requirements.txt

# Core
requests>=2.31.0
flask>=3.0.0
flask-cors>=4.0.0
websocket-client>=1.7.0

# Data (v9 upgrade: Polygon replaces yfinance)
# polygon.io — get free key at https://polygon.io
# yfinance kept as fallback only
yfinance>=0.2.37
pandas>=2.0.0
numpy>=1.24.0

# Broker
alpaca-trade-api>=3.0.0    # optional, we use raw requests
robin_stocks>=2.1.0        # Robinhood fallback

# Utilities
python-dotenv>=1.0.0
pytz>=2024.1

# Optional: ML for crash detection (Phase 3)
# scikit-learn>=1.3.0
# xgboost>=2.0.0

# Optional: Alerts
# twilio>=8.0.0             # SMS alerts
# sendgrid>=6.0.0           # Email alerts

# Optional: OpenAI sentiment explanation
openai>=1.0.0

# Optional: Local FinBERT sentiment scoring. Heavy on small servers; install only if needed.
# transformers>=4.40.0
# torch>=2.2.0

# Backtesting (ktrade_vectorbt.py / ktrade_intraday_vectorbt.py)
vectorbt>=0.26.0

# Tests
pytest>=8.0.0

# Schema validation (v10.7: scan JSON, manual orders, OHLCV frames)
pydantic>=2.7.0
