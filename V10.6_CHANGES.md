# KTrade v10.6 — Changes (on top of your v10.5)

Applies the v10.5 code review. Built on YOUR v10.5 — sector/correlation cap
preserved. Full mapping: docs/REVIEW_RESPONSE_v10.6.md.
Tests: 22/22 + 9/9 (sector) + 6/6 (v10.6) + price-sanity, all pass.

## Fixed
1. `--once` now fetches real data (batch_get), seeds references, runs one real cycle.
2. Price-sanity: references seeded from data_map each cycle; first BUY with no
   reference is refused (KTRADE_REQUIRE_PRICE_REF=true default).
3. Real equity baseline: equity_open set from broker truth on first sync +
   loss_guard reset; risk budget uses real equity, not ACCOUNT_VALUE.
   (Fixes the false "lost 50%" halt when account != ACCOUNT_VALUE.)
4. Dashboard /buy /sell /cancel: admin-token required + bad-tick price gate on
   manual BUY (full RiskEngine wrap still pending — keep submission OFF).
5. Backend security: CORS restricted to KTRADE_ALLOWED_ORIGINS; binds
   KTRADE_BIND_HOST (default 127.0.0.1); KTRADE_ADMIN_TOKEN on order endpoints.
6. run-paper-trade.ps1 rewritten scanner-only (removed missing trader script;
   fixed KTRADE_DATA_SOURCE -> KTRADE_DATA_PROVIDER).
7. All .cmd files use portable `cd /d "%~dp0"`; env var name corrected.
8. requirements.txt: added vectorbt>=0.26.0 and pytest>=8.0.0.
9. agent ask(): added anthropic-version header + timeout=30.

## Manual (your environment)
10. Regenerate stale scan/backtest JSON after `pip install -r requirements.txt`.

## Still open by design
- Full RiskEngine wrap of dashboard orders; real 1-share Alpaca paper round-trip;
  live VIX feed; full walk-forward backtest rebuild.
- Keep KTRADE_PAPER_ORDER_SUBMISSION=false and LIVE_TRADING=false until done.
