# KTrade — V12.7 Changes

Version-consistency fix. No trading-logic changes.

## Fixed
- **Agent now reports a single, consistent version.** Two user-visible strings
  were hardcoded and never tracked `__version__`:
  - the argparse description (shown by `--help`) said "v9.0";
  - a startup log line said "trading_agent_master v8.3".
  Both now derive from `__version__` (the description) or drop the stale number
  (the sub-module line). Added a proper `--version` flag:
  `python agent/ktrade_agent_v9.py --version` → `KTrade PRO Unified Agent v12.7`.
  So bumping `__version__` updates the banner, `--help`, and `--version` together.
- Version bumped to **12.7** across agent `__version__`, dashboard footer, README,
  and `requirements.txt`.

## Verification
- `--help` and the startup banner both show v12.7; zero stale version strings remain.
- Full offline sweep: **12/12 suites** (deterministic, app-data DB cleaned).
- `scripts/check_release_safety.py`: passes.
